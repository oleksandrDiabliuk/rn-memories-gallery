import { StyleSheet } from 'react-native';
import { COLORS } from '../../constants';

export const addNewMemory = StyleSheet.create({
  container: {
    flex: 1,
  },
  wrapper: {
    flex: 1,
    justifyContent: 'center',
    marginTop: 16,
  },
  formContainer: {
    paddingHorizontal: 32,
    paddingBottom: 64,
    // flex: 1,
  },
  formInputs: {
    // flex: 1,
    marginBottom: 32,
  },
});
